Sorts folder full of junk into an output folder where files are sorted into folders based on their file extension.

How to use:

go run main.go ./path/to/folder ./path/to/output

or

go build

./GOTIDY ./path/to/folder ./path/to/output